# workshop

Personal playground for test pages and small projects. Deployed to [workshop.vadstein.dev](https://workshop.vadstein.dev).

Static site, no build step — `index.html` lists whatever lives in this repo. Drop a new project in its own folder and add an entry to the `entries` array in `index.html`.
